parameters:
  kapitan:
    compile:
      - input_paths:
          - fortune/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - fortune/component/main.jsonnet
        input_type: jsonnet
        output_path: fortune/

